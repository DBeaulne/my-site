/** Header component */

// import { useLocation } from "react-router-dom";

function Footer() {
  // const location = useLocation();

  return (
    <footer className="footer">
      <p>danielbeaulne.site</p>
    </footer>
  );
}
export default Footer;